// Задача 1: Создайте объект person, который будет содержать следующие свойства: name (строка), age (число), city (строка). Затем выведите в консоль все свойства объекта.

const person = {
	name: 'Nikita',
	age: 22,
	city: 'Novosibirsk',
};

console.log(person);
